package com.zxl.commands

/**
  * 记录所执行的hql语句
  * Created by ZXL on 2018/2/25.
  */
object hql {
//  "drop table if exists links"
//  "create table if not exists links(movieId int,imdbId int,tmdbId int) stored as parquet"
//  "load data inpath '/tmp/links' overwrite into table links"
}